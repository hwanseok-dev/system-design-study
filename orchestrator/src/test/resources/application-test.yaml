spring:
  datasource:
    url: jdbc:postgresql://localhost:5438/orchestrator
    username: study
    password: study
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    clean-disabled: false
  rabbitmq:
    host: localhost
    port: 5678
    username: admin
    password: admin
  data:
    redis:
      host: localhost
      port: 6378
